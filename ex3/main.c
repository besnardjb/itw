#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    /* Print all arguments in reverse order
    *  also reverse the order of characters in each argument */

    /* Then rerun the *same* program with the reversed arguments */

    return 0;
}